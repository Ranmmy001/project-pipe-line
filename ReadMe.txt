Why sleep 10?

Because:

container needs time to start
without it → ZAP fails
🎯 WHAT YOU CAN NOW SAY IN DEFENSE
❓ “How did you implement DAST?”

“I used OWASP ZAP to scan a running containerized web application exposed on port 3000,
 allowing dynamic security testing of HTTP endpoints.

 “Why did you add a server?”

“DAST requires a running application,
 so I created a minimal Express server to expose HTTP endpoints for security testing.”

❓ “What does it test?”

“It tests runtime vulnerabilities like open ports,
 insecure headers, and exposed endpoints.”

 “Why did you use .gitignore?”

Say:

“I used .gitignore to prevent unnecessary files like dependencies, 
logs, and build artifacts from being pushed to the repository, 
ensuring a clean and efficient version control system.”

Also “It reduces repository size and improves CI/CD performance 
by preventing large or irrelevant files from being tracked.”